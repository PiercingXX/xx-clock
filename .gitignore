# Gradle
.gradle/
build/
app/build/

# Android Studio / IDE
.idea/
*.iml
captures/
.kotlin/

# Local config
local.properties

# Signing material & built artifacts — never commit
*.jks
*.keystore
*.apk
keystore.properties

# Skippy runtime bookkeeping — never tracked
.skippy/

# Estate mill/audit scratch — never tracked
.nagatha/
.bilby/

# Mill-seeded venv is a house symlink or share dir — never tracked
.venv
