<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Privacy: deliberately NO INTERNET permission (mirrors Nope-Mode's verifiable offline claim). -->

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Exact alarms: auto-granted on API 33+ for alarm-clock core purpose; legacy grant below 33. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <!-- Required to make the bypass-DND notification channel effective. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Lets the user opt into ignoring battery optimizations via a direct dialog. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".ClockApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.XxClock">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen alarm alert shown over the lock screen. Pinned to the
             non-DayNight Theme.XxClock.AlarmAlert so its buttons and outlines
             stay readable on the ink ground whatever theme is set, and whatever
             the system is doing. -->
        <activity
            android:name=".ui.AlarmAlertActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:theme="@style/Theme.XxClock.AlarmAlert"
            android:turnScreenOn="true" />

        <!-- Setup / permissions onboarding screen. -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:label="@string/setup_title" />

        <!-- Rings alarms and timers; carries the CATEGORY_ALARM full-screen-intent notification. -->
        <service
            android:name=".service.RingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-scheduled alarm and timer ringing; must play alarm audio and show full-screen alert." />
        </service>

        <!-- Single entry receiver for fire/snooze/dismiss + boot/time-change reconciliation. -->
        <receiver
            android:name=".receiver.AlarmEventReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Family theme sync: receives the xx-launcher's theme broadcast
             (explicitly targeted at this package; exported so a cross-app
             broadcast can reach it — receiving carries no data out, so the
             no-INTERNET privacy claim is untouched). -->
        <receiver
            android:name=".theme.ThemeSyncReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="xx.launcher.THEME_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Home-screen digital clock widget (TextClock based, self-updating). -->
        <receiver
            android:name=".widget.DigitalWidgetProvider"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="com.piercingxx.xxclock.action.REFRESH_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/clock_widget_info" />
        </receiver>
    </application>

</manifest>
